/* bsd_bridge.h -- forwarder. libc_shim.c is compiled in unmodified and
 * includes this by name; everything it declares lives in compat_stubs.h.
 * See the header comment there. */
#ifndef PPS_FWD_BSD_BRIDGE_H
#define PPS_FWD_BSD_BRIDGE_H
#include "compat_stubs.h"
#endif
