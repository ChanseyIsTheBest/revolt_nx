/* sj_paths.h -- forwarder. libc_shim.c is compiled in unmodified and includes
 * this by name; sj_home() and friends are declared in compat_stubs.h.
 * MIT licensed. */
#ifndef RVNX_SJ_PATHS_H
#define RVNX_SJ_PATHS_H
#include "compat_stubs.h"
#endif
